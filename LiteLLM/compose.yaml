services:
  open-webui:
    image: open-webui
    container_name: open-webui

    ports:
      - "3000:8080"

    volumes:
      - /Volumes/Nexus/Documents/Docker/OpenWebui/data:/app/backend/data

    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434

    restart: always

